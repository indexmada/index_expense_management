<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="action_call_journal_caisse" model="ir.actions.server">
            <field name="name">Journal de caisse</field>
            <field name="model_id" ref="index_expense_management.model_pos_session"/>
            <field name="binding_model_id" ref="index_expense_management.model_pos_session"/>
            <field name="state">code</field>
            <field name="code">action = model.call_action_journal_caisse()</field>
            <!-- <field name="code">
                for record in records:
                    record.call_action_journal_caisse()
            </field> -->
        </record>

        <record id="view_pos_session_form_inherit" model="ir.ui.view">
            <field name="name">pos.session.form.view.inherit</field>
            <field name="model">pos.session</field>
            <field name="inherit_id" ref="point_of_sale.view_pos_session_form"/>
            <field name="arch" type="xml">
                <data>
                    <field name="cash_control" position="after">
                        <field name="expense_management" invisible="1"/>
                    </field>

                    <xpath expr="//field[@name='cash_register_balance_start']" position="after">
                        <field name="total_input" attrs="{'invisible': [('state', 'not in', ['opened', 'closing_control'])]}" class="oe_bold oe_pos_total"/>
                        <field name="total_journal_expenses" attrs="{'invisible': [('state', 'not in', ['opened', 'closing_control'])]}" class="oe_bold oe_pos_total"/>
                    </xpath>

                    <xpath expr="//sheet/div/button[2]" position="attributes">
                        <attribute name="attrs">{'invisible': ['|', ('state', 'not in', ['opened', 'closing_control']), '&amp;', ('expense_management', '=', False), ('cash_control', '=', False)]}</attribute>
                    </xpath>
                    <xpath expr="//sheet/div/button[1]" position="before">
                        <!-- <button 
                            class="oe_stat_button" 
                            name="save_expense" 
                            type="object" 
                            icon="fa-pencil-square-o" 
                            attrs="{
                                'invisible': [
                                    ('state', 'not in', ['opened', 'closing_control']),
                                ]}">
                            <div class="o_field_widget o_stat_info">
                                <span class="o_stat_text">Enregistrer</span>
                                <span class="o_stat_text">Dépenses</span>
                            </div>
                        </button> -->
                        <button class="oe_stat_button" name="get_all_expenses" type="object" icon="fa-bars" attrs="{'invisible': ['|', ('expense_management', '=', False), ('state', 'not in', ['opened', 'closing_control'])]}">
                            <div class="o_field_widget o_stat_info">
                                <span class="o_stat_text">Détails des</span>
                                <span class="o_stat_text">Dépenses</span>
                            </div>
                        </button>
                    </xpath>
                </data>
            </field>
        </record>

    </data>
</odoo>
